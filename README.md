# character_sheet

A simple learning project for Flutter. Basically, an app for dynamic forms, aimed especially at TTRPG character sheets, so that through a config file that defines the UI layout, components, and data bindings, and a data file that contains the actual data, one could arbitrarily customize a UI built of simple components and keep different UIs functional with the same data source.
